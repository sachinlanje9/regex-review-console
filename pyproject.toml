[project]
name = "regex-review-console"
version = "0.1.0"
description = "Review bank-statement regexes and record per-regex feedback (chdb + streamlit)"
readme = "QUICKSTART.md"
requires-python = ">=3.12"
dependencies = [
    "chdb>=4.2.1",
    "pandas>=2.2",
    "streamlit>=1.48",
]
